综合说明：KAG — 任务 DAG、执行上下文 与 参考文本块（Chunk）检索关键类与函数（合并版）

说明
- 本文档将之前关于“从任务 DAG 生成任务执行上下文（Context）”和“基于任务 DAG 产生参考文本块（Chunk）”两部分内容合并，并补充关键接口与调用流程的整合视角，便于在实践中将任务调度与检索链路对接。
- 文档保存为 docs/kag_execution_and_chunk.docx（纯文本内容）。如需真正的 Word 二进制格式（.docx），可在后续替换。

目录
1. 总览
2. 任务 DAG -> 执行上下文（Context）相关类与关键函数
3. 基于任务 DAG 的参考文本块（Chunk）检索相关类与关键函数
4. 集成调用流程（任务 DAG -> Context -> Retriever -> Generator）
5. 注意事项与最佳实践
6. 附录：参考文件路径

1. 总览
- KAG 的求解流程通常包含：Planner（生成任务 DAG） -> 将 DAG 解析为 Task 列表并构建执行上下文 Context -> 按拓扑/并行执行任务（Executor/Retriever/Generator） -> 最终生成答案。
- 在该链路中，许多 Task 依赖对外部文档/片段（Chunk）的检索，检索器（Retriever）负责从向量库、搜索 API 或知识图谱回溯出最相关的文本块，并以统一的 RetrieverOutput 向上层返回结果。
- 本文档把任务调度层（Task/Planner/Context/Pipeline）与检索器层（Chunk Retriever）结合描述，重点列出关键类、方法签名、输入/输出与集成示例。

2. 任务 DAG -> 执行上下文（Context）相关类与关键函数

2.1 Task（kag/interface/solver/planner_abc.py）
- 作用：表示单个执行任务，包含 executor 名称、参数、父子依赖、执行结果与内存。
- 关键字段：id (str), executor (str), arguments (dict), parents (List[Task]), children (List[Task]), result (Any), memory (dict)
- 关键方法：
  - create_tasks_from_dag(task_dag: Dict[str, dict]) -> List[Task]
    - 输入：task_dag（dict），每个节点需包含：executor、dependent_task_ids（list）、arguments
    - 输出：List[Task]（已建立 parent/child 关系）
    - 异常：若引用不存在 task id，会触发 KeyError / ValueError
  - add_parent(parent_task: Task) -> None
  - add_child(child_task: Task) -> None
  - update_result(value: Any) -> None
  - update_memory(key: str, value: Any) -> None

2.2 PlannerABC（kag/interface/solver/planner_abc.py）
- 作用：抽象规划器，负责从 query 生成任务 DAG（Task 列表）或基于 LLM 的 JSON plan
- 关键方法：
  - invoke(query: str, **kwargs) -> List[Task]  # 同步
  - ainvoke(query: str, **kwargs) -> List[Task]  # 异步
  - decompose_task(task: Task, **kwargs) -> List[Task]

2.3 Context（kag/interface/solver/context.py）
- 作用：管理 Task 集合、构建依赖 DAG、按拓扑/并行分组产出执行序列
- 关键字段：_tasks: Dict[str, Task]
- 关键方法：
  - add_task(task: Task) -> None
  - append_task(task: Task) -> None  # 若无父则自动把 parent 设为 last_task
  - get_task(task_id: str) -> Optional[Task]
  - last_task() -> Optional[Task]
  - get_dag() -> networkx.DiGraph
    - 构造逻辑：节点为 task.id，边为 parent.id -> task.id
    - 异常：若依赖引用不存在，raise ValueError("task X not found.")
  - gen_task(group: bool=False) -> generator
    - group=False: 按拓扑顺序逐个 yield Task
    - group=True: yield List[Task]（每组内可并行执行）

2.4 SolverPipeline（kag/solver/pipeline/*）
- 作用：编排 planner、executor、generator 的高层流水线
- 常见方法：
  - planning(self, query, context, **kwargs) -> List[Task]
  - execute_task(self, query, task, context, **kwargs) -> None
  - ainvoke(self, query, **kwargs) -> Any
- 示例实现：index_pipeline、naive_rag_pipeline、mcp_pipeline、self_cognition_pipeline

3. 基于任务 DAG 的参考文本块（Chunk）检索相关类与关键函数

3.1 数据模型（kag/interface/common/model/retriever_data.py）
- ChunkData
  - 字段：content (str), title (str), chunk_id (str), score (float), properties (dict)
  - 方法：to_dict() -> dict
- DocData
  - 字段：content (str), title (str), doc_id (str), score (float)

3.2 RetrieverOutput（kag/interface/solver/retriever_abc.py）
- 结构：graphs, chunks (List[ChunkData]), docs (List[DocData]), retriever_method (str), summary (str), err_msg (str), task (Task)
- 方法：to_dict() -> dict

3.3 RetrieverABC（接口）
- 关键方法：invoke(self, task: Task, **kwargs) -> RetrieverOutput
  - 输入：task（含 task.arguments，比如 query、top_k），kwargs 可包含 top_k
  - 返回：RetrieverOutput（包含 chunks 或 err_msg）
- schema(self) -> dict

3.4 主要检索器实现
- VectorChunkRetriever (vector_chunk_retriever.py)
  - init: vectorize_model, search_api, top_k, score_threshold
  - invoke(self, task, **kwargs) -> RetrieverOutput
    - 流程：cache -> vectorize -> search_vector(content/name) -> 合并/去重/排序 -> 封装 ChunkData 列表
- TextChunkRetriever (text_chunk_retriever.py)
  - invoke(self, task, **kwargs) -> RetrieverOutput (基于 search_api.search_text)
- VectorChunkRetrieverLegacy
  - invoke(self, query, top_k) -> Dict[node_id -> {score, content, name}]
- PprChunkRetriever (ppr_chunk_retriever.py)
  - calculate_pagerank_scores(self, start_nodes: List[EntityData], top_k) -> dict
  - invoke: 结合 pagerank 与检索结果合并排序，返回 RetrieverOutput
- AtomicQueryChunkRetriever (atomic_query_chunk_retriever.py)
  - recall_doc_by_atomic_query(self, atomic_query) -> Optional[ChunkData]
  - parse_chosen_atom_infos(self, context: Context) -> List[ChunkData]
- OutlineChunkRetriever (outline_chunk_retriever.py)
  - get_outlines(self, query, top_k) -> List[str]
  - get_children_outlines(self, outline_ids) -> set
  - get_chunk_data(self, chunk_id, score=0.0) -> ChunkData
  - get_related_chunks(self, outline_ids) -> List[ChunkData]

4. 集成调用流程（任务 DAG -> Context -> Retriever -> Generator）

4.1 总体流程：
1) Planner 生成 task_dag（dict）或 LLM 返回的 JSON plan
2) tasks = Task.create_tasks_from_dag(task_dag)
3) ctx = Context(); for t in tasks: ctx.add_task(t)
4) 对于每个并行 generation：
   - 并行执行 generation 内的 tasks
   - 每个 task 的执行器（例如 Retriever）会被选中并被调用（retriever.invoke(task, top_k=...))
   - retriever 返回 RetrieverOutput，包含 chunks（List[ChunkData]），上层把它写回 task.result 或 context
5) 当所有 task 执行完毕后，Generator 从 context 汇总结果并生成最终答案

4.2 伪代码整合：
    tasks = Task.create_tasks_from_dag(task_dag)
    ctx = Context()
    for t in tasks:
        ctx.add_task(t)

    for task_group in ctx.gen_task(group=True):
        # 并发执行 task_group
        await asyncio.gather(*[ execute_task(task, ctx) for task in task_group ])

    # execute_task 示例实现
    async def execute_task(task, ctx):
        if task.executor in retriever_registry:
            retriever = retriever_registry[task.executor]
            output = retriever.invoke(task, top_k=10)
            if output.err_msg:
                task.update_result(None)
                task.update_memory('retriever_err', output.err_msg)
            else:
                task.update_result(output.chunks)
        else:
            # 调用其它类型 executor
            pass

    answer = await generator.ainvoke(query, ctx)

5. 注意事项与最佳实践
- DAG 校验：确保 Planner/LLM 输出满足格式要求（每个节点包含 executor, dependent_task_ids:list, arguments:dict），static_planning_prompt.parse_response 已做基础校验并转换为 Task 列表。
- 循环检测：Context.get_dag 在拓扑排序时会抛 NetworkXUnfeasible，规划器应避免生成循环或在检测到环时重试/降级。
- 并发执行：每代内任务可并发；但跨代必须等待依赖完成。使用 asyncio.gather 或线程池根据执行器是否为 CPU/IO 密集选择。
- 检索器输出契约：统一使用 RetrieverOutput，避免直接依赖 legacy dict 返回格式；上层执行器应对 err_msg 做判断与重试。
- 缓存与性能：检索器普遍内置缓存（chunk_cached_by_query_map），注意缓存大小与 TTL 配置；在批量场景建议聚合向量化请求以降低调用成本。
- 错误与降级策略：当向量检索失败或返回稀少结果时，建议降级到 text retriever 或返回空结果并记录 err_msg 供后续分析。
- 安全与隔离：若检索器或 executor 访问外部服务（数据库/API），请在并发场景下保证连接/会话的线程/协程安全。

6. 附录：参考文件路径（仓库）
- Task/Planner/Context
  - kag/interface/solver/planner_abc.py
  - kag/interface/solver/context.py
  - kag/solver/pipeline/index_pipeline.py
  - kag/solver/pipeline/naive_rag_pipeline.py
  - kag/solver/pipeline/mcp_pipeline.py
  - kag/solver/pipeline/self_cognition_pipeline.py
- Chunk Retriever
  - kag/interface/common/model/retriever_data.py
  - kag/interface/solver/retriever_abc.py
  - kag/common/tools/algorithm_tool/chunk_retriever/vector_chunk_retriever.py
  - kag/common/tools/algorithm_tool/chunk_retriever/vector_chunk_retriever_legacy.py
  - kag/common/tools/algorithm_tool/chunk_retriever/text_chunk_retriever.py
  - kag/common/tools/algorithm_tool/chunk_retriever/ppr_chunk_retriever.py
  - kag/common/tools/algorithm_tool/chunk_retriever/atomic_query_chunk_retriever.py
  - kag/common/tools/algorithm_tool/chunk_retriever/outline_chunk_retriever.py

结束。
